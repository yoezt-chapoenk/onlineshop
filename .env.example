# Copy to .env.local for local development. Set the same variables in Vercel
# Project Settings → Environment Variables for the deployed site.
#
# When unset, the storefront falls back to the static seed in
# src/lib/products.ts so `npm run dev` keeps working without Supabase.

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi... # anon public key
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...     # service role (server only)

# REQUIRED in production. The canonical public origin used to build
# Supabase auth email-confirmation and password-reset links. If this
# is unset in production, register/forgot-password actions return an
# error rather than trusting Host/X-Forwarded-Host headers, which
# would otherwise let an attacker steal the auth code via a forged
# Host: evil.com request. No trailing slash.
NEXT_PUBLIC_SITE_URL=https://your-domain.example.com

# Admin dashboard (HTTP Basic Auth gate at /admin and /api/admin/*).
# If either is unset, /admin returns 503.
ADMIN_BASIC_AUTH_USER=admin
ADMIN_BASIC_AUTH_PASSWORD=change-me-in-vercel

# Biteship (shipping rates API). Obtain from https://biteship.com
# dashboard → Integrations → API Keys. The key is server-only
# (never exposed to the browser).
BITESHIP_API_KEY=biteship_test.xxx

# Cloudflare R2 (S3-compatible object storage) for product images.
# Created via Cloudflare Dashboard → R2 → Create bucket + Manage R2 API
# Tokens. The token must be scoped to this bucket only with Object
# Read & Write permission. R2_PUBLIC_URL is the bucket's public URL —
# either the auto-generated `pub-<hash>.r2.dev` subdomain or a custom
# domain you've connected in the bucket Settings → Public Access tab.
# When unset, the admin upload endpoint returns 503 and the storefront
# falls back to the procedural GlassesArt SVG.
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=...
R2_SECRET_ACCESS_KEY=...
R2_BUCKET=juragan-grosir-products
R2_PUBLIC_URL=https://images.your-domain.example.com
